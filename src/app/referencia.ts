export interface Referencia {
    titulo: string;
    link: string;
    tag: string;
    preview: Preview;
}

export interface Preview {
    title: string;
    description: string;
    image: string;
    url: string;
}
