export interface Referencias {
    titulo: string;
    link: string;
    tag: string;
    preview: Preview;
}

export interface Preview {
    titulo: string;
    description: string;
    image: string;
    url: string;
}
